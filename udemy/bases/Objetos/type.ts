(()=>{

    let flash: { name:string, age: number, powers: string[], getName?:(x:void)=>string } = {
        name: 'Barry Allen',
        age: 24,
        powers: ['Super velocidad', 'Viajar en el tiempo']
    } //Se establece el tipo de flash automaticamente, por lo que solo recibe las propiedades que se especificaron aqui

    let superman: { name:string, age: number, powers: string[], getName?:(x:void)=>string } = {
        name: 'Clark Kent',
        age: 60,
        powers: ['Super velocidad']
    }
    




})()